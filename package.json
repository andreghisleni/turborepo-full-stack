{
  "name": "full-stack",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel --",
    "start": "turbo run start --",
    "build": "turbo run db:generate; turbo run build --",
    "lint": "turbo run lint --",
    "format": "turbo run format --"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "pnpm@9.0.6",
  "engines": {
    "node": ">=18"
  }
}
